# Core runtime dependencies — everything the SQLite-based local dev
# setup actually needs. Same exact versions that were here before;
# only psycopg2-binary, gunicorn, and redis moved to
# requirements-prod.txt.
#
# Production / Docker deployments install both files. See
# deployment/README.md.

# --- Core framework ---
Django==5.2.13
asgiref==3.11.1
sqlparse==0.5.5
tzdata==2026.2
typing_extensions==4.15.0

# --- REST API ---
djangorestframework==3.15.2
django-cors-headers==4.3.1

# --- Field-level encryption ---
django-encrypted-model-fields==0.6.5
cryptography==47.0.0
cffi==2.0.0
pycparser==3.0

# --- Static file serving ---
# whitenoise is referenced unconditionally in hospital_project/settings.py
# (MIDDLEWARE + STORAGES), so it must be installed even in the dev path
# where Django serves static via runserver.
whitenoise==6.7.0
